---
# Update the apt cache if it is older than the apt_cache_valid_time value.
# Option set in seconds.
apt_cache_valid_time: 3600

dropbear_initramfs__ssh_port: 22

dropbear_initramfs__authorized_ssh_keys:
  - example
